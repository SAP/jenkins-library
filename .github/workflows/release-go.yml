name: Build and Release

on:
  schedule:
    - cron:  '*/2 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Prepare Release
        run: |
          curl --insecure --silent --location --write-out '%{http_code}' --output ./piper https://github.com/SAP/jenkins-library/releases/latest/download/piper_master
          cp ./piper_master ./piper
          npm install semver
          echo "::set-env name=PIPER_version::v$(node_modules/.bin/semver -i minor $(curl --silent "https://api.github.com/repos/$GITHUB_REPOSITORY/releases/latest" | jq -r .tag_name))"
      - uses: SAP/project-piper-action@master
        with:
          piper-version: latest
          command: githubPublishRelease
          flags: --token ${{ secrets.GITHUB_TOKEN }} --owner fwilhe --assetPath ./piper_master
      - uses: SAP/project-piper-action@master
        with:
          piper-version: latest
          command: githubPublishRelease
          flags: --token ${{ secrets.GITHUB_TOKEN }} --owner fwilhe --version latest --assetPath ./piper
