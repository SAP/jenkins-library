metadata:
    name: abapAddonAssemblyKitCheckCVs
    description: TODO
    longDescription: |
        TODO    
spec:
  inputs:
    secrets:
      - name: abapCredentialsId
        description: Credential for AbapAddonAssemblyKit System
        type: jenkins
    params:
      - name: AbapAddonAssemblyKitEndpoint
        type: string
        description: TODO
        scope:
        - PARAMETERS
        - STAGES
        - STEPS
        - GENERAL
        mandatory: true
        default: https://w7q.dmzwdf.sap.corp/odata/aas_ocs_package
      - name: username
        type: string
        description: User
        scope: 
        - PARAMETERS
        - STAGES
        - STEPS
        mandatory: true
        secret: true
      - name: password
        type: string
        description: User Password
        scope:
        - PARAMETERS
        - STAGES
        - STEPS
        mandatory: true
        secret: true
      - name: addonDescriptorFileName
        type: string
        description: File name of the YAML descriptor
        mandatory: true
        default: .pipeline/addon.yml
  outputs:
    resources:
      - name: commonPipelineEnvironment
        type: piperEnvironment
        params:
          - name: abap/repositories