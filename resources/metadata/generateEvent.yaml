metadata:
  name: generateEvent
  description: generateEvent
  longDescription: |
spec:
  inputs:
    params:
      - name: vaultAppRoleID
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultAppRoleSecretID
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultBasePath
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultPipelineName
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultNamespace
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultToken
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
      - name: vaultServerUrl
        type: "string"
        description:
        scope:
          - GENERAL
          - PARAMETERS
          - STAGES
          - STEPS
