#Project Setup
general:
  projectName: 'my-project'

#Steps Specific Configuration
steps:
  mavenExecute:
    dockerImage: 'maven:3.6-jdk-8-alpine'

#Stage Specific Configurations
stages:
  artifactDeployment:
    nexus:
      version: nexus3
      url: localhost:8081
      repository: maven-releases
